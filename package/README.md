# kk-calculator

A tiny sample library (`add`, `subtract`, `multiply`, `safe_divide`) published to an
**Azure Artifacts** feed to demonstrate Python package management.

```python
from kkcalc import add, safe_divide
add(2, 3)          # 5
safe_divide(10, 2) # 5.0
```
